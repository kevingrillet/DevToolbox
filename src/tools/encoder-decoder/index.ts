/**
 * Point d'entrée de l'outil Encodeur / Décodeur, chargé en `lazy()` par le
 * routeur (un chunk JS dédié). Le `default` est le composant racine de l'outil.
 */
export { default } from './EncoderPage';
