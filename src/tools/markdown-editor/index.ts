/**
 * Point d'entrée de l'éditeur Markdown, chargé en `lazy()` par le routeur (chunk
 * dédié, qui embarque marked + dompurify). Le `default` est le composant racine.
 */
export { default } from './MarkdownEditorPage';
