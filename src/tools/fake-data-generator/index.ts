/**
 * Point d'entrée du générateur de données factices, chargé en `lazy()` par le
 * routeur (chunk dédié). Le `default` est le composant racine de l'outil.
 */
export { default } from './FakerPage';
