/**
 * Point d'entrée de l'outil Hash / Checksum, chargé en `lazy()` par le routeur
 * (chunk dédié). Le `default` est le composant racine de l'outil.
 */
export { default } from './HashChecksumPage';
